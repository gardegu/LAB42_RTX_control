#ifndef MAKE_PATH_H
#define MAKE_PATH_H
#include "basics.h"

static int distance(/*data elt, chess_move_tm *move*/);
static element_t *expand(/* data parent, ourboardT board, chess_move_t *cm */);
#endif
